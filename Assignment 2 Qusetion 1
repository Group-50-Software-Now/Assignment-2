# Encryption.py

def shift_char(char, shift):
    if char.islower():
        base = ord('a')
        return chr((ord(char) - base + shift) % 26 + base)
    elif char.isupper():
        base = ord('A')
        return chr((ord(char) - base + shift) % 26 + base)
    return char
